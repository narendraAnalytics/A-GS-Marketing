GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest
# Restricts LangGraph checkpoint deserialization to a safe allowlist derived
# from the graph's own state schema (mitigates CVE-2026-28277).
LANGGRAPH_STRICT_MSGPACK=true
